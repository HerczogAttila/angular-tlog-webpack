export class MyDate {
  type: string; // empty, simple, work
  year: number;
  month: number;
  day: number;
  requiredWorkMinutes = 0;
  minutes = 0;
  extraMinutes = 0;
  weekend = false;
  tasks: any[] = [];
}
